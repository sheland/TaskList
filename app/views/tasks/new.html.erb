<%= form_with model: @task, class: "task-form" do |f| %>
  <p>Please add a new task:</p>

  <%= f.label :chore %>
  <%= f.text_field :chore %>

  <%= f.submit "Save Task", class: "book-button" %>
<% end %>


<%= form_with scope: :article, url: articles_path, local: true do |form| %>
  ...
<% end %>

<%= link_to 'Back', new_task_path %>
